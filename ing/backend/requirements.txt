# API
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # uploads multipart en /consulta/imagen

# Retrieval TF-IDF (backend por defecto, liviano)
scikit-learn>=1.3
numpy>=1.24

# Retrieval E5 (opcional, calidad paper) — descomentar para usar DERMA_RETRIEVER=e5
# torch>=2.2
# transformers>=4.45

# Retrieval multimodal (opcional, texto+imagen) — para DERMA_RETRIEVER=multimodal.
# Suma el encoder visual BiomedCLIP sobre lo anterior. Corre en CPU (no requiere GPU).
# open_clip_torch>=2.20
# pillow>=10.0

# Tests y lint
pytest>=8.0
httpx>=0.27
ruff>=0.6
